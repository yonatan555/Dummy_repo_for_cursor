import React from 'react';
import { Typography, Box } from '@mui/material';

const Payments = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        ניהול תשלומים
      </Typography>
      <Typography variant="body1">
        דף ניהול תשלומים - בפיתוח
      </Typography>
    </Box>
  );
};

export default Payments;