import React from 'react';
import { Typography, Box } from '@mui/material';

const ResidentPayments = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        התשלומים שלי
      </Typography>
      <Typography variant="body1">
        דף תשלומים דיירים - בפיתוח
      </Typography>
    </Box>
  );
};

export default ResidentPayments;